# Backend (placeholder)

This folder is intentionally empty — the FastAPI backend is being built
separately by a teammate. See the root README for the API contract the
frontend expects (`/api/v1/users/admin-exists`, `/api/v1/users/register`,
`/api/v1/auth/login`, etc.) and the `NEXT_PUBLIC_API_URL` env var the
frontend uses to reach it.


